import React from "react";
import "./NavBar.css";

const NavBar = () => {
  return (
    <div className="sidenav">
      <a href="#">Home</a>
      <a href="#">Profile</a>
      <a href="#">Create Post</a>
      <a href="#">Sign Out</a>
    </div>
  );
};

export default NavBar;
